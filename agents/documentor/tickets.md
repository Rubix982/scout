# Documentor Tickets

No open tickets.

Pending work, not yet ticketed: `README.md` still documents Scout as a cold-outreach
engine across all 8 steps (32KB). It is now the primary source of confusion in the
repo — a second engineer reading it would build the wrong system. A `D-` ticket should
rewrite it against `plan.md` once E-003/E-004 land and the ingestion shape is settled;
rewriting it before then would document intentions rather than behaviour.

[pytest]
# Project root, not `src`: tests import `src.clients` / `src.db`, which needs the
# root on sys.path. `pythonpath = src` put the package's *contents* on the path
# instead, so those imports could only resolve by accident.
pythonpath = .
testpaths = tests
